extends layout

block content

  form.ui.form(action="/room/create" method="POST")
    .field
      label Room name
      input(type="text" name="room-name" placeholder="Enter Room name")
      button.ui.button(type="submit") Create Room
    
